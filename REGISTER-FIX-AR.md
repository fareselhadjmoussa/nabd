# إصلاح مشكلة إنشاء الحساب

من مجلد `backend` شغّل:

```powershell
npm run fix:indexes
npm run dev
```

ثم اختبر التسجيل ببريد جديد. إذا فشل، ستظهر الآن رسالة `error` أو `duplicatedField` توضّح السبب الحقيقي.
